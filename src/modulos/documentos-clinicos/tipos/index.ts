import type { EstadoDocumentoClinico, TipoDiagnosticoClinico } from "@/generated/prisma/enums";
export interface DocumentoClinicoResumenDto { id: string; numeroDocumento: string; trabajadorId: string; trabajador: string; documento: string; fecha: string; empresa: string; profesional: string; estado: EstadoDocumentoClinico; }
export interface DocumentoClinicoDetalleDto extends DocumentoClinicoResumenDto { fechaNacimiento: string | null; departamento: string | null; motivoConsulta: string | null; evolucion: string | null; observaciones: string | null; registroDiarioId: string | null; diagnosticos: Array<{ id: string; codigo: string; descripcion: string; tipo: TipoDiagnosticoClinico; observacion: string | null }>; tratamientos: Array<{ id: string; nombre: string; concentracion: string | null; dosis: string; cantidad: string; frecuencia: string | null; intervaloHoras: number | null; duracion: string | null; via: string | null; indicaciones: string | null; observaciones: string | null }>; motivoAnulacion: string | null; }

